[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools", "wheel"]

[project]
name = "asweb"
authors = [{name = "Tillmann Stübler", email = "ts109@pm.me"}]
dependencies = ["wrenfold"]
requires-python = ">= 3.14"
